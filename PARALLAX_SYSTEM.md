# 🎬 PARALLAX HERO VIDEO SYSTEM

## Опис
Система фіксованого фонового відео з паралакс ефектом для всіх hero секцій сайту.

## Принцип роботи
Відео залишається фіксованим на фоні (`position: fixed`), а контент скролиться поверх нього.

## Реалізовані сторінки

### ✅ Home (`/`)
- **Hero**: `.hero-section` з фіксованим відео
- **Перша секція**: `.services-section` з rounded top та тінню
- **Ефект**: Контент скролиться поверх відео з анімацією

### ✅ Blog (`/blog/`)
- **Hero**: `.blog-hero` з фіксованим відео
- **Перша секція**: `.blog-header` з rounded top та тінню
- **Ефект**: Статті скроляться поверх відео блогу

### ✅ Events (`/events/`)
- **Hero**: `.hero-section` з фіксованим відео
- **Перша секція**: `.events-filter` з rounded top та тінню
- **Ефект**: Події скроляться поверх відео

### ✅ Portfolio (`/portfolio/`)
- **Hero**: `.portfolio-hero` з фіксованим відео
- **Перша секція**: `.portfolio-projects` з rounded top та тінню
- **Ефект**: Проекти скроляться поверх відео

## Z-Index Ієрархія

```
-1   → hero-video, video-overlay (найнижче - фон)
 0   → hero-section (скролиться)
 3   → hero-content (контент на hero)
10   → sections після hero (скролиться поверх відео)
1000 → header (навігація завжди зверху)
10000→ mobile-menu (найвище)
```

## CSS Файли

### Створені:
- `static/css/components/parallax-hero.css` - загальна система паралаксу

### Оновлені:
- `static/css/home.css` - паралакс для головної
- `static/css/blog.css` - паралакс для блогу
- `static/css/events.css` - паралакс для подій
- `static/css/portfolio.css` - паралакс для портфоліо
- `templates/base.html` - підключення parallax-hero.css

## Ключові стилі

```css
/* Фіксоване відео */
.video-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    object-fit: cover;
}

/* Перша секція після hero */
.content-section-first {
    position: relative;
    background: #090407;
    z-index: 10;
    border-radius: 20px 20px 0 0;
    margin-top: -20px;
    box-shadow: 0 -10px 30px rgba(0, 0, 0, 0.3);
}
```

## Мобільна адаптація

### Desktop (1025px+)
- Повний паралакс ефект
- Border-radius: 20px
- Повна тінь

### Mobile (< 767px)
- Спрощений паралакс
- Border-radius: 16px
- Адаптований padding

### iOS Safari
- Підтримка `calc(var(--vh, 1vh) * 100)`
- Safe areas підтримка
- Оптимізація для landscape

## Збережені функції

✅ **Прозорість header** - header з `z-index: 1000` завжди поверх контенту
✅ **Mobile menu** - меню з `z-index: 10000` найвище
✅ **Scroll handlers** - всі scroll ефекти працюють
✅ **Video lazy loading** - оптимізоване завантаження відео
✅ **Responsive відео** - desktop/mobile версії

## Особливості

### Без конфліктів:
- ❌ Немає `!important`
- ❌ Немає inline стилів
- ✅ Всі стилі в CSS файлах
- ✅ Чистий каскад

### Оптимізація:
- GPU-прискорення через `position: fixed`
- Мінімум reflow/repaint
- Адаптивні медіа-кверії
- Fallback градієнти

## Результат

🎬 **Ефект**: Відео залишається фіксованим на фоні, контент плавно скролиться поверх
📱 **Адаптивність**: Працює на всіх пристроях включаючи iOS Safari
⚡ **Продуктивність**: Оптимізовано для плавної роботи
🎨 **Дизайн**: Елегантний rounded top з тінню на першій секції

