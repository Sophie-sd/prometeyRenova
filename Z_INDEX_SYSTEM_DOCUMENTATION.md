# 🎯 Z-INDEX СИСТЕМА - ДОКУМЕНТАЦІЯ

## 📋 Огляд

Це єдина централізована система управління z-index для всього проєкту. Вона гарантує відсутність конфліктів видимості контенту та правильну ієрархію елементів.

## 🏗️ Архітектура

### Рівні Z-Index (від найнижчого до найвищого):

```
-1     : Фонові елементи (відео, зображення)
0-1    : Фонові overlay
10     : Базовий контент
50     : Піднятий контент
100    : Hero контент
500    : Dropdown меню
800    : Tooltips
999    : Фіксовані кнопки
1000   : Навігація
10000  : Мобільне меню
10001  : Кнопка burger menu
2000   : Modal backdrop
2001   : Modal content
```

## 📦 CSS Змінні

### Файл: `static/css/core/z-index-system.css`

```css
:root {
    /* Фонові елементи */
    --z-background: -1;
    --z-video-background: 0;
    --z-overlay-background: 1;
    
    /* Основний контент */
    --z-content-base: 10;
    --z-content-elevated: 50;
    --z-content-hero: 100;
    
    /* Навігація */
    --z-navigation: 1000;
    --z-mobile-menu: 10000;
    --z-burger-button: 10001;
    
    /* UI елементи */
    --z-dropdown: 500;
    --z-tooltip: 800;
    --z-fixed-buttons: 999;
    
    /* Модальні вікна */
    --z-modal-backdrop: 2000;
    --z-modal-content: 2001;
}
```

## 🎬 Система Відео Фонів

### Hero Секції (Фіксоване позиціонування)

**Використання:** Перша секція на сторінці з паралакс ефектом

```css
.hero-section {
    z-index: var(--z-content-base);
    position: relative;
}

.hero-section .video-background {
    position: fixed;
    z-index: var(--z-background);
}

.hero-content {
    z-index: var(--z-content-hero);
    pointer-events: auto;
}
```

### CTA Секції (Абсолютне позиціонування)

**Використання:** Всі інші секції з відео на тій же сторінці

```css
.cta-section {
    z-index: var(--z-content-base);
    position: relative;
    isolation: isolate;  /* ВАЖЛИВО! */
}

.cta-section .video-background {
    position: absolute;  /* НЕ fixed! */
    z-index: 1;
}

.cta-content {
    z-index: 100;
    pointer-events: auto;
}
```

## 🔧 Правила Використання

### ✅ МОЖНА:

1. **Використовувати CSS змінні з fallback:**
   ```css
   z-index: var(--z-navigation, 1000);
   ```

2. **Одна секція з fixed відео на сторінці:**
   - Hero секція використовує `position: fixed`
   
3. **Інші секції з absolute відео:**
   - CTA та інші використовують `position: absolute`
   - Обов'язково додати `isolation: isolate`

### ❌ НЕ МОЖНА:

1. **Жорстко вказувати z-index:**
   ```css
   /* ПОГАНО */
   z-index: 100;
   
   /* ДОБРЕ */
   z-index: var(--z-content-hero, 100);
   ```

2. **Декілька fixed відео на одній сторінці:**
   ```css
   /* ПОГАНО - створює конфлікт */
   .hero-section .video-background { position: fixed; }
   .cta-section .video-background { position: fixed; }
   ```

3. **Inline стилі для z-index або height:**
   ```javascript
   // ПОГАНО
   element.style.zIndex = '100';
   element.style.height = '100vh';
   ```

## 📁 Файли З Z-Index

### Core файли:
- ✅ `static/css/core/z-index-system.css` - Центральна система

### Компоненти:
- ✅ `static/css/components/hero-parallax.css` - Hero секції
- ✅ `static/css/components/navigation.css` - Навігація
- ✅ `static/css/components/buttons.css` - Кнопки
- ✅ `static/css/components/modals.css` - Модальні вікна

### Сторінки:
- ✅ `static/css/home.css` - CTA секція
- ✅ `static/css/portfolio.css` - Portfolio grid
- ✅ `static/css/blog.css` - Blog hero

## 🐛 Вирішення Проблем

### Контент не видно:

1. **Перевірте z-index:**
   ```css
   /* Контент повинен мати вищий z-index ніж фон */
   .content { z-index: var(--z-content-hero); }
   ```

2. **Перевірте pointer-events:**
   ```css
   .content { pointer-events: auto; }
   ```

3. **Для секцій з absolute відео додайте isolation:**
   ```css
   .section { isolation: isolate; }
   ```

### Конфлікт відео:

1. **Тільки hero має fixed відео:**
   ```css
   .hero-section .video-background { position: fixed; }
   ```

2. **Інші секції мають absolute:**
   ```css
   .cta-section .video-background { position: absolute; }
   ```

### JavaScript проблеми:

1. **НЕ встановлювати inline стилі:**
   ```javascript
   // ПОГАНО
   element.style.height = '100vh';
   
   // ДОБРЕ - використовувати CSS класи
   element.classList.add('full-height');
   ```

## 🔍 Перевірка Системи

### Швидкий тест:

```bash
# Знайти всі жорстко вказані z-index (крім системних файлів)
grep -r "z-index:" static/css --exclude-dir=core | grep -v "var(--z-"
```

### Очікуваний результат:
- Мінімальна кількість результатів
- Всі використовують CSS змінні

## 📚 Приклади Використання

### Створення нової секції з відео:

```css
.my-section {
    position: relative;
    z-index: var(--z-content-base);
    isolation: isolate;
}

.my-section .video-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
}

.my-section .content {
    position: relative;
    z-index: 100;
    pointer-events: auto;
}
```

### Додавання нового UI елемента:

```css
.my-tooltip {
    position: fixed;
    z-index: var(--z-tooltip, 800);
}
```

## 🎓 Навчання Команди

### Перед додаванням z-index запитайте себе:

1. ❓ Чи можу я використати існуючу CSS змінну?
2. ❓ Чи правильно я розумію ієрархію елементів?
3. ❓ Чи не створю я конфлікт з іншими елементами?
4. ❓ Чи додав я fallback значення?

### Якщо потрібен новий рівень:

1. Додайте змінну в `z-index-system.css`
2. Задокументуйте її призначення
3. Оновіть цей файл документації

## 🚀 Оптимізація Продуктивності

### Isolation context:

```css
/* Створює новий stacking context */
.section {
    isolation: isolate;
}
```

**Переваги:**
- Ізолює z-index всередині секції
- Запобігає конфліктам між секціями
- Покращує продуктивність браузера

### Pointer events:

```css
/* Дозволяє взаємодію з контентом */
.content {
    pointer-events: auto;
}
```

**Переваги:**
- Гарантує кліки по кнопках
- Дозволяє виділення тексту
- Робить форми доступними

## 📝 Changelog

### 2025-10-03 - Створення системи
- ✅ Створено централізовану z-index систему
- ✅ Розділено fixed і absolute відео
- ✅ Додано isolation contexts
- ✅ Оновлено всі компоненти
- ✅ Видалено inline стилі з JS
- ✅ Створено документацію

## 🎯 Підсумок

Ця система забезпечує:
- ✅ Відсутність конфліктів z-index
- ✅ Передбачувану ієрархію елементів
- ✅ Легку підтримку та розширення
- ✅ Краще розуміння коду командою
- ✅ Захист від майбутніх проблем

---

**Важливо:** Завжди використовуйте цю систему при додаванні нових елементів!

