/**
 * ANALYTICS.JS — kept as a stable defer slot from base.html.
 *
 * Meta Pixel / Ads / Clixtell live only in GTM-K2FVPPTK (base.html _loadGTM).
 * CSS media="print" → all swap lives only in the inline <head> script.
 * Do not duplicate either here: a second swap makes in-flight sheets blocking.
 */
